
<dir id="conteudo">
  <h1>Telefone</h1>

<%= form_for(@telefone) do |f| %>
  <% if @telefone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@telefone.errors.count, "error") %> prohibited this telefone from being saved:</h2>

      <ul>
      <% @telefone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numero %><br>
    <%= f.text_field :numero %>
  </div>
  <div class="field">
    <%= f.label :contato_id %><br>
    <%= collection_select(:telefone, :contato_id, @listarContatos, :id, :nome) %>
  </div>
  <div class="actions">
    <%= link_to 'Voltar', telefones_path, :class => "btn btn-info"  %>
    <%= f.submit 'Salvar', :class => "btn btn-success" %>
  </div>
<% end %>

</dir>  
